## Output
```
┌── Vector Rendering
├ Rendering Circle in a vector format.
├ Rendering Square in a vector format.
└ Rendering Triangle in a vector format.

┌── Raster Rendering
├ Rendering Circle in a raster format.
├ Rendering Square in a raster format.
└ Rendering Triangle in a raster format.
```